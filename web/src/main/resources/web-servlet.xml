<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:utils="http://www.springframework.org/schema/util"
       xsi:schemaLocation="http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd http://www.springframework.org/schema/util http://www.springframework.org/schema/util/spring-util.xsd">

    <!--
        DispatcherServlet Context: defines this servlet's request-processing
        infrastructure
    -->
    <context:annotation-config/>

    <!-- TODO re-check usage of this bean -->
    <bean id="web.FlashMapManager" class="mobi.nowtechnologies.server.web.view.CookieFlashMapManager"/>

    <!-- Configures the Spring MVC @Controller programming model -->
    <mvc:annotation-driven>
        <mvc:argument-resolvers>
            <bean class="org.springframework.mobile.device.site.SitePreferenceWebArgumentResolver" />
        </mvc:argument-resolvers>
    </mvc:annotation-driven>

    <mvc:resources mapping="/assets/**" location="/assets/**" />

    <utils:list id="tilesLocation">
        <value>/WEB-INF/tiles-defs/template-defs.xml</value>
    </utils:list>




    <bean id="web.tilesConfigurer" class="org.springframework.web.servlet.view.tiles2.TilesConfigurer">
        <property name="definitions" ref="tilesLocation"/>

        <!-- Initialise expression language support for use in Tiles definitions. -->
        <property name="tilesProperties">
            <props>
                <prop key="org.apache.tiles.evaluator.AttributeEvaluator">org.apache.tiles.el.ELAttributeEvaluator</prop>
            </props>
        </property>
        <property name="preparerFactoryClass" value="org.springframework.web.servlet.view.tiles2.SpringBeanPreparerFactory" />
    </bean>

    <bean id="web.tilesViewResolver" class="org.springframework.web.servlet.view.UrlBasedViewResolver">
        <property name="viewClass" value="org.springframework.web.servlet.view.tiles2.TilesView" />
    </bean>
    
    <bean id="cdnProperties" class="org.springframework.beans.factory.config.PropertiesFactoryBean">
		<property name="locations">
			<list>
				<value>classpath:cdn.properties</value>
				<value>file:#{systemProperties['catalina.base']}/conf/#{systemProperties['env']}/cdn.properties</value>
			</list>
		</property>
        <property name="ignoreResourceNotFound" value="true" />
	</bean>

    <bean id="web.CommunityViewPreparer" class="mobi.nowtechnologies.server.web.view.CommunityViewPreparer">
    	<property name="cdnProperties" ref="cdnProperties" />
    </bean>

    <!-- Application Message Bundle -->
    <bean id="messageSource"
          class="org.springframework.context.support.ReloadableResourceBundleMessageSource">
        <property name="basenames" value="file:#{systemProperties['catalina.base']}/conf/#{systemProperties['env']}/i18n/web/messages, /i18n/messages" />
        <property name="defaultEncoding" value="UTF-8" />
        <property name="fileEncodings" value="UTF-8" />
        <property name="cacheSeconds" value="180" />
        <property name="useCodeAsDefaultMessage" value="true" />
    </bean>

    <bean id="localeResolver"
          class="org.springframework.web.servlet.i18n.CookieLocaleResolver">
        <property name="defaultLocale" value="en" />
        <property name="cookieName" value="_LOCALE" />
    </bean>

    <mvc:interceptors>
        <!-- Community Change Interceptor and Resolver definition -->
        <bean id="communityChangeInterceptor" class="mobi.nowtechnologies.server.web.security.CommunityChangeInterceptor">
            <property name="propLocale" ref="propLocale"/>
        </bean>

        <mvc:interceptor>
            <mvc:mapping path="/**"/>
            <bean class="org.springframework.web.servlet.mvc.WebContentInterceptor">
                <property name="cacheSeconds" value="0"/>
                <property name="useExpiresHeader" value="true"/>
                <property name="useCacheControlHeader" value="true"/>
                <property name="useCacheControlNoStore" value="true"/>
            </bean>
        </mvc:interceptor>

        <mvc:interceptor>
            <mvc:mapping path="/assets/**"/>
            <bean class="org.springframework.web.servlet.mvc.WebContentInterceptor">
                <!-- Cache for one week of all assets -->
                <property name="cacheSeconds" value="604800"/>
            </bean>
        </mvc:interceptor>

        <bean class="mobi.nowtechnologies.server.shared.web.interceptor.LoggerInterceptor">
            <property name="userRepository" ref="userRepository"/>
        </bean>
    </mvc:interceptors>

    <import resource="classpath:META-INF/placeholders.xml"/>
    <import resource="controllers.xml" />
    <bean id="JacksonView" class="org.springframework.web.servlet.view.ContentNegotiatingViewResolver">
        <property name="order" value="1"/>
        <property name="mediaTypes">
            <map>
                <entry key="json" value="application/json"/>
            </map>
        </property>

        <property name="defaultViews">
            <list>
                <bean class="org.springframework.web.servlet.view.json.MappingJackson2JsonView">
                </bean>
            </list>
        </property>
    </bean>

    <beans profile="test">
        <utils:list id="tilesLocation">
            <value>classpath:/WEB-INF/tiles-defs/template-defs.xml</value>
        </utils:list>
    </beans>
</beans>