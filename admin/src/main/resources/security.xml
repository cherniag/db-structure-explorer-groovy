<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:sec="http://www.springframework.org/schema/security"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security.xsd">

	<sec:http pattern="/admin/*" security="none"/>
	<sec:http pattern="/MANIFEST.MF" security="none" />
	
	<sec:http create-session="stateless" use-expressions="true">

		<sec:intercept-url pattern="/assets/**" access="permitAll" method="GET" />
		<sec:intercept-url pattern="/signup**" access="permitAll" />
		<sec:intercept-url pattern="/signin**" access="permitAll" />
		<sec:intercept-url pattern="/signout" access="permitAll" />
		<sec:intercept-url pattern="/restore_password**" access="permitAll" />

		<sec:intercept-url pattern="/**" access="authenticated" />

		<sec:custom-filter before="FIRST" ref="communityResolverFilter" />
		
		<sec:http-basic />

		<sec:form-login default-target-url="/" authentication-failure-url="/signin?error" login-page="/signin" password-parameter="token" username-parameter="email"
			login-processing-url="/login" />

		<sec:logout logout-success-url="/signin" logout-url="/signout" delete-cookies="JSESSIONID" />

		<sec:remember-me key="admin" services-ref="nowTechBasedRememberMeServices" use-secure-cookie="true" />

		<sec:custom-filter after="REMEMBER_ME_FILTER" ref="afterRememberMeFilter" />

	</sec:http>

	<bean id="securityDataSource" class="org.springframework.jdbc.datasource.DriverManagerDataSource">
		<property name="driverClassName" value="${admin.db.driverClassName}" />
		<property name="url" value="${admin.db.url}" />
		<property name="username" value="${admin.db.username}" />
		<property name="password" value="${admin.db.password}" />
	</bean>

	<sec:authentication-manager alias="authenticationManager">
		<sec:authentication-provider user-service-ref="security.nowTechUserDetailsService">
			<sec:password-encoder hash="md5">
				<sec:salt-source user-property="username" />
			</sec:password-encoder>
		</sec:authentication-provider>
	</sec:authentication-manager>
	
	<bean id="nowTechBasedRememberMeServices" class="mobi.nowtechnologies.server.security.NowTechTokenBasedRememberMeServices">
		<constructor-arg>
			<value>admin</value>
		</constructor-arg>
		<constructor-arg>
			<ref bean="security.nowTechUserDetailsService" />
		</constructor-arg>
		<property name="cookieName" value="${rememberMe.cookieName}"/>
		<property name="alwaysRemember" value="true"/>
	</bean>

	<bean id="security.nowTechUserDetailsService" class="mobi.nowtechnologies.server.admin.security.service.impl.AdminDetailsServiceImpl">
		<property name="dataSource" ref="securityDataSource" />
	</bean>

	<bean id="communityResolverFilter" class="mobi.nowtechnologies.server.shared.web.filter.CommunityResolverFilter" />

	<bean id="afterRememberMeFilter" class="mobi.nowtechnologies.server.shared.web.security.filter.AfterRememberMeFilter">
		<property name="nowTechTokenBasedRememberMeServices">
			<ref bean="nowTechBasedRememberMeServices" />
		</property>
	</bean>
</beans>