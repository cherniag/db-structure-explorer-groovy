#!/bin/bash

CUR_DIR=`dirname $0`

INPUT_FILE=${2}
AUDIO_RATE=${11}
OUTPUT_FILE=${15}

echo $CUR_DIR

case $AUDIO_RATE in
		48)
			INPUT="$CUR_DIR/APPCAST_48.m4a";;
		96) 			
			INPUT="$CUR_DIR/APPCAST_96.m4a";;
esac

cp $INPUT $OUTPUT_FILE